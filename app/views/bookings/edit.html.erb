<h1>Editing Booking</h1>

<%= render 'form', booking: @booking %>

<%= link_to 'Delete', @booking, :confirm => "Are you sure?", :method => :delete, :class => 'btn btn-info deleteButton' %>
<%= link_to 'Back', '/week', :class => 'btn btn-info backbutton' %>
